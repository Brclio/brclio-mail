# 安全策略

## 支持范围

Brclio Mail 当前只有 **Preview**，没有生产支持分支或安全修复 SLA。只有仓库最新版本会被评估；旧 commit、fork、修改版和未受支持部署配置可能要求先在隔离环境复现。

Preview 不应承载关键业务、唯一邮件副本或合规归档。已公开的功能缺口见[限制与路线图](docs/limitations-roadmap.md)和[威胁模型](docs/threat-model.md)。

## 私下报告

请使用 GitHub 的私有漏洞报告入口：

https://github.com/Brclio/brclio-mail/security/advisories/new

不要先创建公开 issue、discussion、pull request 或可被搜索的演示实例。报告请包含：

- 受影响 commit、版本和部署方式；
- 可最小化复现的步骤或测试，但不要包含真实用户邮件/secret；
- 实际与预期结果、攻击前置条件和影响；
- 是否需要网络、认证、管理员或主机权限；
- 已做的任何临时缓解；
- 你希望采用的署名方式。

请对日志、截图和数据库样本做不可逆脱敏。不要发送 TLS/DKIM/relay 私钥、setup token、应用密码或真实邮件原文；若最小复现必须交换敏感材料，先在 advisory 中协商安全渠道。

维护者会尽力确认收到、复现、评估影响并协调修复/披露，但 Preview 阶段不承诺固定响应或发布时间。请给出合理修复窗口后再公开。

## 优先关注

- 未认证读取邮件、附件、Bcc/envelope 元数据、归档或审计；
- 非管理员获得管理员功能，或绕过归档原因/审计写入；
- SMTP 开放中继、Submission From 所有权绕过；
- TLS/认证降级、会话伪造、CSRF/XSS、路径遍历或任意文件读取；
- 密码、session、setup token、DKIM/TLS/relay secret 泄露；
- 恶意 MIME 导致代码执行、持久脚本执行或跨用户数据访问；
- SQLite 注入、越权修改、备份完整性绕过或远程拒绝服务；
- 容器/CI 供应链问题。

以下已知缺口本身通常不是新漏洞，但若存在绕过已声明边界或影响超出文档，请报告：未实现入站 SPF/DKIM/DMARC 验证、反垃圾/杀毒、MTA-STS、应用层静态加密、高可用和完整 DSN。普通用户删除已收发或非草稿导入的邮件后，管理员仍可读取是明确产品行为；尚未发送的私有草稿不进入管理员归档。未授权普通用户读取、未审计管理员归档访问或披露之外的数据泄露仍是安全问题。

## 部署者的即时响应

发生疑似泄露时，先隔离并保存证据，再撤销应用密码/会话，轮换 setup、relay、TLS 和相应 DKIM 密钥，检查管理员审计、域/用户/别名和投递队列，并评估法定通知。详细步骤见[运维文档](docs/operations.md)。

不要把攻击流量导向他人系统，不要访问与验证漏洞无关的真实邮件，也不要以测试为由破坏可用性或保留数据。
